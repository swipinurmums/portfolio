const loader=document.querySelector('.loader');const photos=[...document.querySelectorAll('.photo')];const viewer=document.querySelector('.viewer');const viewerImage=document.querySelector('.viewer__image');const viewerCounter=document.querySelector('.viewer__counter');const backdrop=document.querySelector('.viewer__backdrop');const leftZone=document.querySelector('.viewer__zone--left');const centreZone=document.querySelector('.viewer__zone--centre');const rightZone=document.querySelector('.viewer__zone--right');let currentIndex=0,touchStartX=0,touchStartY=0;window.addEventListener('load',()=>setTimeout(()=>loader.classList.add('is-hidden'),250));const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');revealObserver.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(item=>revealObserver.observe(item));function sourceFor(index){const img=photos[index].querySelector('img');return{src:img.currentSrc||img.src,alt:img.alt}}function renderViewer(){const image=sourceFor(currentIndex);viewerImage.src=image.src;viewerImage.alt=image.alt;viewerCounter.textContent=`${String(currentIndex+1).padStart(2,'0')} / ${String(photos.length).padStart(2,'0')}`}function openViewer(index){currentIndex=index;renderViewer();viewer.classList.add('is-open');viewer.setAttribute('aria-hidden','false');document.body.classList.add('is-locked')}function closeViewer(){viewer.classList.remove('is-open');viewer.setAttribute('aria-hidden','true');document.body.classList.remove('is-locked')}function nextImage(){currentIndex=(currentIndex+1)%photos.length;renderViewer()}function previousImage(){currentIndex=(currentIndex-1+photos.length)%photos.length;renderViewer()}photos.forEach((photo,index)=>photo.addEventListener('click',()=>openViewer(index)));leftZone.addEventListener('click',previousImage);rightZone.addEventListener('click',nextImage);centreZone.addEventListener('click',closeViewer);backdrop.addEventListener('click',closeViewer);viewer.addEventListener('touchstart',event=>{const touch=event.changedTouches[0];touchStartX=touch.clientX;touchStartY=touch.clientY},{passive:true});viewer.addEventListener('touchend',event=>{const touch=event.changedTouches[0];const dx=touch.clientX-touchStartX,dy=touch.clientY-touchStartY;if(Math.abs(dx)>55&&Math.abs(dx)>Math.abs(dy)){dx<0?nextImage():previousImage()}},{passive:true});window.addEventListener('keydown',event=>{if(!viewer.classList.contains('is-open'))return;if(event.key==='ArrowRight')nextImage();if(event.key==='ArrowLeft')previousImage();if(event.key==='Escape')closeViewer()});
